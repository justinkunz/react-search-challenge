import styled from 'styled-components';

const AgeLocationText = styled.span`
  font-family: 'Open Sans', sans-serif;
  font-size: 14px;
  font-weight: 600;

  @media (max-width: 428px) {
    font-size: 12px;
  }
`;

export default AgeLocationText;
